services:
  changedetection:
    image: ghcr.io/dgtlmoon/changedetection.io:latest
    container_name: changedetection
    ports:
      - "${CHANGEDETECTION_PORT}:5000"
    volumes:
      - /opt/data/changedetection:/datastore
    restart: unless-stopped
